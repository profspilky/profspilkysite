__pycache__/
*.py[cod]
*.pyo
.env
.env.*
db.sqlite3
media/
*.pem
*.key
staticfiles/
.DS_Store
*.log
logo.png

# SQL dumps and large data files — never commit
# All raw import data lives in tools/data/ (git-ignored except .gitkeep, README.md, fixtures)
tools/data/*
!tools/data/.gitkeep
!tools/data/README.md
!tools/data/fixtures.json.gz

# Data archive for deployment (generated locally, uploaded to GitHub Releases)
fpu_data.tar.gz

# Generated tool artifacts — regeneratable, not needed in git
tools/image_paths.txt
